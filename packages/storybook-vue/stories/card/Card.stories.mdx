import { Meta, Story, Props, Preview, Description } from '@storybook/addon-docs/blocks';
import ScaleCard from './ScaleCard.vue';
import prosa from './card.md';

<!--
  👀

  We're missing two key things from Stencil's docs in the generated readme file:
  - whether attributes are optional or required
  - any description for those attributes
  - plus, writing them again in the .vue file can be error-prone.

  Other minor pain-points:
  - writing the code-snippet manually, need to make sure attribute/values match
  - declaring all props both in Template and the <template> of the .vue file

  Two more thing to think about:
  - in the end there's nothing Vue-specific in the stories, only for us devs, no?
    al least until the code snippet in the Preview works as expected and doesn't show `Template.bind()`
  - we're not using the Vue output target package (it's not really useful in Storybook)
-->

<Meta
  title='Card'
  component={ScaleCard}
  argTypes={{
    label: {
      control: { type: 'text' }
    },
    styles: {
      control: { disable: true }
    }
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleCard },
  props: {
    label: String,
    ...ScaleCard.props
  },
  template: `
    <scale-card
      :custom-class="customClass"
      :href="href"
      :target="target"
      :interactive="interactive"
      :disabled="disabled"
      :styles="styles"
    >
      {{ label }}
    </scale-card>
  `
})

<Description>{prosa}</Description>

<Preview>
  <Story
    name='Standard'
    args={{
      label: 'A card'
    }} 
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story='Standard' />

## With Link

<Preview>
  <Story
    name='With Link'
    args={{
      label: 'A card with a link to example.com',
      href: 'https://example.com',
      target: '_blank'
    }} 
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-card href="https://example.com" target="_blank">A card with a link to example.com</scale-card>
```

## Interactive

<Preview>
  <Story
    name='Interactive'
    args={{
      label: 'A card, interactive, no link',
      interactive: true
    }} 
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-card interactive>A card, interactive, no link</scale-card>
```

## Disabled

<Preview>
  <Story
    name='Disabled'
    args={{
      label: 'A card, disabled',
      disabled: true
    }} 
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-card disabled>A card, interactive</scale-card>
```

## Long text

<Preview>
  <Story
    name='Long text'
    args={{
      label: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.'
    }} 
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-card>Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.</scale-card>
```
