import { Meta, Story, Props, Preview, Description } from '@storybook/addon-docs/blocks';
import ScaleBreadcrumb from './ScaleBreadcrumb.vue';

<Meta
  title='Components/Breadcrumb'
  component={ScaleBreadcrumb}
  argTypes={{
    separator: {
      control: { type: "text" }
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleBreadcrumb },
  props: {
    label: String,
    ...ScaleBreadcrumb.props
  },
  template: `
    <scale-breadcrumb
      :separator="separator"
      :styles="styles"
    >
      <a href="#1">First Entry</a>
      <a href="#2">Second Entry</a>
      <a href="#3">Active Page</a>
      <template v-if="separator === true" #separator>
        <span slot="separator">
          <scale-icon-navigation-right color="#6B6B6B"></scale-icon-navigation-right>
        </span>
      </template>
    </scale-breadcrumb>
  `
})

# Breadcrumb

## Standard

<Preview withSource="none">
  <Story
    name='Standard'
    args={{}}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story='Standard' />

```html
<scale-breadcrumb separator="---">
  <a href="#1">First link</a>
  <a href="#2">Second link</a>
  <a href="#3">Third link</a>
</scale-breadcrumb>
```

## With Custom Separator Element

<Preview withSource="none">
  <Story
    name='With Custom Separator Element'
    args={{
      separator: true
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-breadcrumb>
  <a href="#1">First link</a>
  <a href="#2">Second link</a>
  <a href="#3">Third link</a>
  <span slot="separator">
    <scale-icon-navigation-right></scale-icon-navigation-right>
  </span>
</scale-breadcrumb>
```
