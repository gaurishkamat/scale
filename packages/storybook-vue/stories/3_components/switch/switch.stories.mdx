import { Meta, Story, Props, Preview, Description } from '@storybook/addon-docs/blocks';
import ScaleSwitch from './ScaleSwitch.vue';
import description from './switch.md';
const ICONON = 'M21.15,4.85 L9,17.05 L3.3,11.3 C2.9,10.9 2.3,10.9 1.9,11.3 C1.5,11.7 1.5,12.3 1.9,12.7 L9,19.85 L22.6,6.25 C23,5.85 23,5.25 22.6,4.85 C22.2,4.45 21.55,4.5 21.15,4.85 Z';
const ICONOFF = 'M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z';

<Meta
title="Components/Switch"
component={ScaleSwitch}
argTypes={{
    styles: {
        control: {disable: true}
    }
}}
/>

export const Template = (args, {argTypes}) => ({
    components: {ScaleSwitch},
    props: {
        ...ScaleSwitch.props
    },
    template: `
    <scale-switch
        :active="active"
        :custom-class="customClass"
        :disabled="disabled"
        :icon-off="iconOff"
        :icon-on="iconOn"
        :icon-size="iconSize"
        :text-off="textOff"
        :text-on="textOn"
        >
    </scale-switch>
    `
})

<Description>{description}</Description>

<Preview withSource="none">
    <Story
        name="Standard">
        {Template.bind({})}
    </Story>
</Preview>

<Props story="Standard"/>

## Switch with icon

<Preview withSource="none">
    <Story
    name="A switch with icon"
    args={{
        iconOn: ICONON,
        iconOff: ICONOFF
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-switch icon-on="SVG-PATH" icon-off="SVG-PATH"></scale-switch>
```

## Active switch disabled

<Preview withSource="none">
    <Story
    name="Active switch disabled"
    args={{
        disabled: true,
        active: true
    }}
    >
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-switch disabled=true active=true></scale-switch>
```

## Inactive switch disabled

<Preview withSource="none">
    <Story
    name="Inactive switch disabled"
    args={{
        disabled: true,
        active: false
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-switch disabled=true active=false></scale-switch>
```
