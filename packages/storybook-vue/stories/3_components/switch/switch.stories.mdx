import { Meta, Story, Props, Preview, Description } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import ScaleSwitch from './ScaleSwitch.vue';

<Meta
  title="Components/Switch"
  component={ScaleSwitch}
  argTypes={{
    styles: {
      control: {disable: true}
    }
  }}
/>

export const Template = (args, {argTypes}) => ({
  components: {ScaleSwitch},
  props: ScaleSwitch.props,
  template: `
    <scale-switch
      :custom-class="customClass"
      :disabled="disabled"
      :input-id="inputId"
      :checked="checked"
      :label="label"
      @scaleChange="scaleChange"
    >
    </scale-switch>
  `,
  methods: {
    scaleChange: action('scaleChange'),
  },
})

# Switch

## Standard

<Preview withSource="none">
  <Story
    name="Standard"
    args={{
      label: 'Standard'
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story="Standard"/>


## Standard disabled

<Preview withSource="none">
  <Story
    name="Standard disabled"
    args={{
      disabled: true,
      label: 'Standard disabled'
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-switch disabled @scaleChange="handleChange" label="Standard disabled"></scale-switch>
```


## Selected

<Preview withSource="none">
  <Story
    name="Selected"
    args={{
      checked: true,
      label: 'Selected'
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-switch checked @scaleChange="handleChange" label="Selected"></scale-switch>
```


## Selected disabled

<Preview withSource="none">
    <Story
      name="Selected disabled"
      args={{
        disabled: true,
        checked: true,
        label: 'Selected disabled'
      }}
    >
      {Template.bind({})}
    </Story>
</Preview>

```html
<scale-switch disabled checked @scaleChange="handleChange" label="Selected disabled"></scale-switch>
```
