import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import ScaleLink from './ScaleLink.vue';
export const ICON = 'M12.65 3l1.5 1.5H4.5V18c0 .85.65 1.5 1.5 1.5h12c.85 0 1.5-.65 1.5-1.5V9.85l1.5 1.5V18c0 1.65-1.35 3-3 3H6c-1.65 0-3-1.35-3-3V3zM22.7 1.35l-1.65 7.9-2.6-2.6-7.55 7.55c-.15.15-.35.2-.55.2s-.4-.05-.55-.2c-.3-.3-.3-.75 0-1.05l7.55-7.55-2.6-2.6 7.95-1.65z';

<Meta
    title="Components/Link"
    component={ScaleLink}
    argTypes={{
        label: {
            control: {type: 'text'}
        },
        styles: {
            control: {disable: true}
        }
    }}
/>

export const Template = (args, {argTypes}) => ({
    components: {ScaleLink},
    props: {
        label: String,
        ...ScaleLink.props,
    },
    template: `
        <scale-link
        :block="block"
        :custom-lass="customClass"
        :disabled="disabled"
        :href="href"
        :icon="icon"
        :icon-size="iconSize"
        :icon-accessibility-title="iconAccessibilityTitle"
        :target="target"
        :variant="variant"
    >
    {{ label }}
    </scale-link>
    `
})

<div style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'space-between', width: '100%'}}>
    <h1>Link</h1>
    <img src="assets/aa.png" alt="Accessible AA" />
</div>

## Standard

<Canvas withSource="none">
    <Story
        name="Standard"
        args={{
            label: 'A link',
            href: '#top'
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Standard"/>

## Disabled

<Canvas withSource="none">
    <Story
        name="Disabled"
        args={{
            label: 'A link, disabled',
            disabled: true,
            href: '#top'
        }}
    >
    {Template.bind({})}
    </Story>
</Canvas>

```html
<scale-link disabled href="#top">A link, disabled</scale-link>
```


## With Icon

<Canvas withSource="none">
    <Story
        name="With Icon"
        args={{
            label: 'A link, with an icon',
            icon: ICON,
            href: '#top',
            iconAccessibilityTitle: 'open link in new tab'
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

```html
<scale-link icon="SVG-PATH" href="#top">A link, with an icon</scale-link>
```
