import { Meta, Story, ArgsTable, Canvas, Description } from '@storybook/addon-docs/blocks';
import ScaleLink from './ScaleLink.vue';
export const ICON = 'M12.65 3l1.5 1.5H4.5V18c0 .85.65 1.5 1.5 1.5h12c.85 0 1.5-.65 1.5-1.5V9.85l1.5 1.5V18c0 1.65-1.35 3-3 3H6c-1.65 0-3-1.35-3-3V3zM22.7 1.35l-1.65 7.9-2.6-2.6-7.55 7.55c-.15.15-.35.2-.55.2s-.4-.05-.55-.2c-.3-.3-.3-.75 0-1.05l7.55-7.55-2.6-2.6 7.95-1.65z';

<Meta
    title="Components/Link"
    component={ScaleLink}
    argTypes={{
        label: {
            control: {type: 'text'}
        },
        styles: {
            control: {disable: true}
        }
    }}
/>

export const Template = (args, {argTypes}) => ({
    components: {ScaleLink},
    props: {
        label: String,
        ...ScaleLink.props,
        underline: true
    },
    template: `
        <scale-link
        :block="block"
        :custom-lass="customClass"
        :disabled="disabled"
        :href="href"
        :icon="icon"
        :icon-size="iconSize"
        :target="target"
        :underline="underline"
        :variant="variant"
    >
    {{ label }}
    </scale-link>
    `
})

# Link

## Standard

<Canvas withSource="none">
    <Story
        name="Standard"
        args={{
            label: 'A link'
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Standard"/>

## Disabled

<Canvas withSource="none">
    <Story
        name="Disabled"
        args={{
            label: 'A link, disabled',
            disabled: true
        }}
    >
    {Template.bind({})}
    </Story>
</Canvas>

```html
<scale-link disabled>A link, disabled</scale-link>
```


## With Icon

<Canvas withSource="none">
    <Story
        name="With Icon"
        args={{
            label: 'A link, with an icon',
            icon: ICON
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

```html
<scale-link icon="SVG-PATH">A link, with an icon</scale-link>
```

## Without Underline

<Canvas withSource="none">
    <Story
        name="Without Underline"
        args={{
            label: 'A link without underline',
            underline: false
        }}>
        { Template.bind({})}
    </Story>
</Canvas>

```html
<scale-link underline="false">A link without underline</scale-link>
```
