import { Meta, Story, Props, Preview, Description } from '@storybook/addon-docs/blocks';
import ScaleTag from './ScaleTag.vue';
import description from './tag.md';

<Meta
  title='Components/Tag'
  component={ScaleTag}
  argTypes={{
    label: {
      control: { type: 'text' }
    },
    styles: {
      control: { disable: true }
    }
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleTag },
  props: {
    label: String,
    ...ScaleTag.props
  },
  template: `
    <scale-tag
    :custom-class="customClass"
    :disabled="disabled"
    :dismissable="dismissable"
    :href="href"
    :size="size"
    :target="target"
    :variant="variant"
    >
    {{ label }}
    </scale-tag>
  `
})


<Description>{description}</Description>

<Preview withSource="none">
  <Story
    name='Standard'
    args={{
      label: 'A tag'
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story="Standard"/>

## A tag with a link

<Preview withSource="none">
    <Story
    name="With link"
    args={{
        href: 'https://www.example.com',
        label: 'A tag with a link'
    }}
    >
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag href="https://www.example.com">Label</scale-tag>
```

## A tag with link target blank

<Preview withSource="none">
    <Story
    name="With link target blank"
    args={{
        href: 'https://www.example.com',
        label: 'A tag with a link target blank',
        target: '_blank'
    }}
    >
        {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag href="https://www.example.com" target="_blank">A tag with a link target blank</scale-tag>
```

## A dissmisable tag

<Preview withSource="none">
    <Story
    name="A dissmisable tag"
    args={{
        label: 'A dissmisable tag',
        dismissable: true
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag dismissable>A dissmisable tag</scale-tag>
```

## A small tag

<Preview withSource="none">
    <Story
    name="A small tag"
    args={{
        label: 'A small tag',
        size: 'small'
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag size="small">A small tag</scale-tag>
```

## A small dismissable tag

<Preview withSource="none">
    <Story
    name="A small dismissable tag"
    args={{
        label: 'A small dismissable tag',
        size: 'small',
        dismissable: true
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag size="small" dismissable>A small dismissable tag</scale-tag>
```

## Disabled tag

<Preview withSource="none">
    <Story
    name="Disabled tag"
    args={{
        label: 'Disabled tag',
        disabled: true
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag disabled>Disabled tag</scale-tag>
```

## Disable dissmisable tag

<Preview withSource="none">
    <Story
    name="Disabled dismissable tag"
    args={{
        label: 'Disabled dismissable tag',
        disabled: true,
        dismissable: true,
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag disabled dismissable>Disabled dismissable tag</scale-tag>
```

## Variant secondary tag

<Preview withSource="none">
    <Story
    name="Variant secondary tag"
    args={{
        label: 'Variant secondary tag',
        variant: 'secondary'
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag variant="secondary">Variant secondary tag</scale-tag>
```

## Variant secondary disabled

<Preview withSource="none">
    <Story
    name="Variant secondary disabled"
    args={{
        label: 'Variant secondary disabled',
        variant: 'secondary',
        disabled: true
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag variant="secondary" disabled>Variant secondary disabled</scale-tag>
```

## Variant secondary link

<Preview withSource="none">
    <Story
    name="Variant secondary link"
    args={{
        label: 'Variant secondary link',
        variant: 'secondary',
        href: 'https://www.example.com',
        target: '_blank'
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag variant="secondary" href="https://www.example.com" target="_blank">Variant secondary link</scale-tag>
```

## Variant secondary dismissable

<Preview withSource="none">
    <Story
    name="Variant secondary dismissable"
    args={{
        label: 'Variant secondary dismissable',
        variant: 'secondary',
        dismissable: true
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag variant="secondary" dismissable>Variant secondary dismissable</scale-tag>
```

## Variant secondary small

<Preview withSource="none">
    <Story
    name="Variant secondary small"
    args={{
        label: 'Variant secondary small',
        variant: 'secondary',
        size: 'small'
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag variant="secondary" size="small">Variant secondary small</scale-tag>
```

## Variant secondary dismissable small

<Preview withSource="none">
    <Story
    name="Variant secondary dismissable small"
    args={{
        label: 'Variant secondary dismissable small',
        variant: 'secondary',
        dismissable: true,
        size: 'small'
    }}>
    {Template.bind({})}
    </Story>
</Preview>

```html
<scale-tag variant="secondary" dismissable size="small">Variant secondary dismissable small</scale-tag>
```
