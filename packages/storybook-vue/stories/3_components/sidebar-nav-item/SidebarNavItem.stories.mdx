import { Meta, Story, Props, Preview, Description } from '@storybook/addon-docs/blocks';
import ScaleSidebarNavItem from './SidebarNavItem.vue';
import description from './sidebar-nav-item.md';

<Meta
  title='Components/Sidebar Nav Item'
  component={ScaleSidebarNavItem}
  argTypes={{
    width: {
      control: {
        type: 'radio',
        options: ['256px', '25rem', '100%']
      },
      defaultValue: '25rem',
      description: 'Example width for wrapper element'
    },
    styles: {
      control: { disable: true }
    }
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleSidebarNavItem },
  props: {
    width: { type: String },
    ...ScaleSidebarNavItem.props,
  },
  computed: {
    wrapperStyles() {
      return {
        width: this.width
      }
    }
  },
  template: `
    <div :style="wrapperStyles">
      <scale-sidebar-nav>
        <scale-sidebar-nav-item
          :condensed="condensed"
          :bold="bold"
          :is-current="isCurrent"
        >
          <a href="#">Overview</a>
        </scale-sidebar-nav-item>
      </scale-sidebar-nav>
    </div>
  `
})

<Description>{description}</Description>

<Preview withSource="none">
  <Story name='Standard' args={{ bold: true }}>
    {Template.bind({})}
  </Story>
</Preview>

<Props story='Standard' />

```html
<scale-sidebar-nav aria-label="primary">
  <scale-sidebar-nav-item bold>
    <a href="#">Overview</a>
  </scale-sidebar-nav-item>
</scale-sidebar-nav>
```