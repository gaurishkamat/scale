import { Meta, Story, Props, Preview, Description } from '@storybook/addon-docs/blocks';
import ScaleButton from "./ScaleButton.vue";
import description from './button.md'
export const ICON = 'M10 2a8 8 0 016.472 12.703l4.912 4.913.06.065a1.25 1.25 0 01-1.726 1.794l-.102-.091-4.913-4.912A8 8 0 1110 2zm0 1.5A6.508 6.508 0 003.5 10c0 3.584 2.916 6.5 6.5 6.5s6.5-2.916 6.5-6.5-2.916-6.5-6.5-6.5z';
import { action } from "@storybook/addon-actions";

<Meta
  title='Components/Button'
  component={ScaleButton}
  argTypes={{
    variant: {
      control: { type: 'select', options: ['primary', 'secondary'] }
    },
    label: {
      defaultValue: 'Label',
      control: { type: 'text' }
    },
    disabled: {
      control: { type: 'boolean' }
    }
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleButton },
  props: {
    label: String,
    ...ScaleButton.props
  },
  template: `
    <scale-button
      :ariaLabel="ariaLabel"
      :disabled="disabled"
      :focusable="focusable"
      :icon="icon"
      :iconAfter="iconAfter"
      :iconBefore="iconBefore"
      :iconSize="iconSize"
      :href="href"
      :role="role"
      :target="target"
      :variant="variant"
      @click="click"
    >
      {{label}}
    </scale-button>
  `,
  methods: { click: action('click') }
})

<Description>{description}</Description>

<Preview withSource="none">
  <Story
    name='Standard'
    args={{
      label: 'A button'
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story='Standard' />


## Secondary

<Preview withSource="none">
  <Story
    name='Secondary'
    args={{
      variant: 'secondary',
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-button variant="secondary">Label</scale-button>
```


## Disabled

<Preview withSource="none">
  <Story
    name='Disabled'
    args={{
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-button disabled>Label</scale-button>
```


## With Icon Before

<Preview withSource="none">
  <Story
    name='With Icon Before'
    args={{
      iconBefore: ICON,
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-button :icon-before="SVG-PATH">Label</scale-button>
```


## With Icon After

<Preview withSource="none">
  <Story
    name='With Icon After'
    args={{
      iconAfter: ICON,
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-button :icon-after="SVG-PATH">Label</scale-button>
```


## Icon Only

<Preview withSource="none">
  <Story
    name='Icon Only'
    args={{
      icon: ICON,
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-button :icon="SVG-PATH">Label</scale-button>
```


## Small Icon

<Preview withSource="none">
  <Story
    name='Small Icon'
    args={{
      icon: ICON,
      iconSize: 16
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-button :icon="SVG-PATH" icon-size="small">Label</scale-button>
```


## Link

<Preview withSource="none">
  <Story
    name='Link'
    args={{
      href: 'https://example.com',
      target: '_blank'
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-button :href="https://example.com" :target="_blank">Label</scale-button>
```
