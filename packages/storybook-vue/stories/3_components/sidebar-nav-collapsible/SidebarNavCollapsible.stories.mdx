import { Meta, Story, Props, Preview, Description } from '@storybook/addon-docs/blocks';
import ScaleSidebarNavCollapsible from './SidebarNavCollapsible.vue';

<Meta
  title='Components/Sidebar Navigation'
  component={ScaleSidebarNavCollapsible}
  argTypes={{
    width: {
      control: {
        type: 'radio',
        options: ['256px', '25rem', '100%']
      },
      defaultValue: '25rem',
      description: 'Example width for wrapper element'
    },
    styles: {
      control: { disable: true }
    }
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleSidebarNavCollapsible },
  props: {
    width: { type: String },
    ...ScaleSidebarNavCollapsible.props,
  },
  computed: {
    wrapperStyles() {
      return {
        width: this.width
      }
    }
  },
  template: `
    <div :style="wrapperStyles">
      <scale-sidebar-nav>
        <scale-sidebar-nav-collapsible
          :tag="tag"
          :label="label"
          :is-expanded="isExpanded"
          :is-current="isCurrent"
          :condensed="condensed"
          :bold="bold"
          :icon-size="iconSize"
        >
          <scale-sidebar-nav-item :condensed="condensed">
            <a href="#">Installation</a>
          </scale-sidebar-nav-item>
          <scale-sidebar-nav-item :condensed="condensed">
            <a href="#">Configuration</a>
          </scale-sidebar-nav-item>
        </scale-sidebar-nav-collapsible>
      </scale-sidebar-nav>
    </div>
  `
})

# Sidebar Navigation

## Standard

<Preview withSource="none">
  <Story name='Standard' args={{ bold: true, label: 'Getting Started' }}>
    {Template.bind({})}
  </Story>
</Preview>

<Props story='Standard' />

```html
<scale-sidebar-nav aria-label="primary">
  <scale-sidebar-nav-collapsible label="Getting Started" bold>
    <scale-sidebar-nav-item>
      <a href="#">Installation</a>
    </scale-sidebar-nav-item>
    <scale-sidebar-nav-item>
      <a href="#">Configuration</a>
    </scale-sidebar-nav-item>
  </scale-sidebar-nav-collapsible>
</scale-sidebar-nav>
```
