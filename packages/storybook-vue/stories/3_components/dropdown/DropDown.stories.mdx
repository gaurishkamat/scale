import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import ScaleDropDown from "./ScaleDropDown.vue";
import { action } from "@storybook/addon-actions";

<Meta
  title='Components/Dropdown'
  component={ScaleDropDown}
  argTypes={{}}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleDropDown },
  props: {
    label: String,
    ...ScaleDropDown.props
  },
  template: `
    <scale-input
      :name="name"
      :variant="variant"
      :type="type"
      :label="label"
      :helper-text="helperText"
      :status="status"
      :disabled="disabled"
      :required="required"
      :placeholder="placeholder"
      :size="size"
      :value="value"
      :icon="icon"
      :input-id="inputId"
      :visible-size="visibleSize"
      :controlled="controlled"
      @scaleChange="scaleChange"
      @scaleFocus="scaleFocus"
      @scaleBlur="scaleBlur"
      @scaleKeyDown="scaleKeyDown"
    >
      <option value="1">Category 1</option>
      <optgroup label="Category Group">
        <option value="2">Category 2</option>
      </optgroup>
      <optgroup label="Category Group 2">
        <option value="3">Category 3</option>
      </optgroup>
    </scale-input>
  `,
  methods: {
    scaleChange: action('scaleChange'),
    scaleFocus: action('scaleFocus'),
    scaleBlur: action('scaleBlur'),
    scaleKeyDown: action('scaleKeyDown'),
  }
})

# Dropdown

## Standard

<Canvas withSource="none">
  <Story
    name='Standard'
    args={{
      label: 'Select',
      type: 'select',
      variant: 'animated',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story='Standard' />

```html
<scale-input icon="SVG-PATH" type="select" label="select">
  <option value="1">item 1</option>
  <option value="2" selected>item 2</option>
  <option value="3">item 3</option>
</scale-input>
```


## Small

<Canvas withSource="none">
  <Story
    name='Small'
    args={{
      label: 'Small',
      type: 'select',
      size: 'small',
      variant: 'animated',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>


```html
<scale-input icon="SVG-PATH" type="select" label="Small" size="small" variant="animated" placeholder="Small">
  <option value="1">item 1</option>
  <option value="2" selected>item 2</option>
  <option value="3">item 3</option>
</scale-input>
```


## Disabled

<Canvas withSource="none">
  <Story
    name='Disabled'
    args={{
      label: 'Disabled',
      type: 'select',
      variant: 'animated',
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>


```html
<scale-input icon="SVG-PATH" type="select" label="Disabled" disabled>
  <option value="1">item 1</option>
  <option value="2" selected>item 2</option>
  <option value="3">item 3</option>
</scale-input>
```


## Error

<Canvas withSource="none">
  <Story
    name='Error'
    args={{
      label: 'Error',
      type: 'select',
      variant: 'animated',
      status: 'error',
      helperText: 'Something went wrong...',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>


```html
<scale-input icon="SVG-PATH" type="select" label="Error" status="error" helper-text="Something went wrong...">
  <option value="1">item 1</option>
  <option value="2" selected>item 2</option>
  <option value="3">item 3</option>
</scale-input>
```
