import { Meta, Story, ArgsTable, Canvas, Description } from "@storybook/addon-docs/blocks";
import ScaleCheckbox from "./ScaleCheckbox.vue";
import { action } from "@storybook/addon-actions";
export const ICON = "M21.15,4.85 L9,17.05 L3.3,11.3 C2.9,10.9 2.3,10.9 1.9,11.3 C1.5,11.7 1.5,12.3 1.9,12.7 L9,19.85 L22.6,6.25 C23,5.85 23,5.25 22.6,4.85 C22.2,4.45 21.55,4.5 21.15,4.85 Z";

<Meta
  title="Components/Checkbox"
  component={ScaleCheckbox}
  argTypes={{}}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleCheckbox },
  props: {
    label: String,
    ...ScaleCheckbox.props
  },
  template: `
    <scale-checkbox
      :name="name"
      :label="label"
      :helper-text="helperText"
      :disabled="disabled"
      :checked="checked"
      :value="value"
      :status="status"
      :input-id="inputId"
      :icon="icon"
      @scaleChange="scaleChange"
    ></scale-checkbox>
  `,
  methods: {
    scaleChange: function() {
      action('scaleChange')();
    },
  },
})

<div style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'space-between', width: '100%'}}>
    <h1>Checkbox</h1>
    <img src="assets/aa.png" alt="Accessible AA" />
</div>


## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      label: "Checkbox",
      icon: ICON
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" icon="SVG-PATH" />


## Standard Disabled

<Canvas withSource="none">
  <Story
    name="Standard Disabled"
    args={{
      label: "Standard Disabled",
      icon: ICON,
      disabled: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-checkbox icon="SVG-PATH" disabled @scaleChange="handleChange"></scale-checkbox>
```

## Selected

<Canvas withSource="none">
  <Story
    name="Selected"
    args={{
      label: "Selected",
      icon: ICON,
      checked: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-checkbox icon="SVG-PATH" checked @scaleChange="handleChange"></scale-checkbox>
```

## Selected Disabled

<Canvas withSource="none">
  <Story
    name="Selected Disabled"
    args={{
      label: "Selected Disabled",
      icon: ICON,
      checked: true,
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-checkbox icon="SVG-PATH" checked disabled @scaleChange="handleChange"></scale-checkbox>
```


## Error

<Canvas withSource="none">
  <Story
    name="Error"
    args={{
      label: "Standard with error",
      icon: ICON,
      status: "error",
      helperText: "This is the error message"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-checkbox icon="SVG-PATH" status="error" @scaleChange="handleChange"></scale-checkbox>
```
