import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks";
import ScaleTextArea from "./ScaleTextArea.vue";
import { action } from "@storybook/addon-actions";

<Meta
  title="Components/Text Area"
  component={ScaleTextArea}
  argTypes={{
    resize: {
      control: {
        type: "select",
        options: ["unset", "none", "vertical", "horizontal"]
      }
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleTextArea },
  props: ScaleTextArea.props,
  template: `
    <scale-textarea
      :name="name"
      :label="label"
      :size="size"
      :rows="rows"
      :cols="cols"
      :helper-text="helperText"
      :status="status"
      :max-length="maxLength"
      :min-length="minLength"
      :placeholder="placeholder"
      :disabled="disabled"
      :required="required"
      :counter="counter"
      :resize="resize"
      :value="value"
      :input-id="inputId"
      :transparent="transparent"
      @scaleChange="scaleChange"
      @scaleFocus="scaleFocus"
      @scaleBlur="scaleBlur"
      @scaleKeyDown="scaleKeyDown"
    ></scale-textarea>
  `,
  methods: {
    scaleChange: action("scaleChange"),
    scaleFocus: action("scaleFocus"),
    scaleBlur: action("scaleBlur"),
    scaleKeyDown: action("scaleKeyDown"),
  }
})

<div style={{display: 'inline-flex', alignItems: 'center', justifyContent: 'space-between', width: '100%'}}>
    <h1>Text Area</h1>
    <img src="assets/aa.png" alt="Accessible AA" />
</div>

## Standard

<Canvas withSource="none">
  <Story
    name="Standard"
    args={{
      label: "Textarea",
      rows: 3,
      resize: "vertical"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Standard" />

```html
<scale-textarea label="Textarea" rows="3" resize="vertical"></scale-textarea>
```



## Placeholder

<Canvas withSource="none">
  <Story
    name="Placeholder"
    args={{
      label: "Placeholder",
      placeholder: "this is the placeholder"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-textarea label="Placeholder" placeholder="this is the placeholder"></scale-textarea>
```


## Helper Text

<Canvas withSource="none">
  <Story
    name="Helper Text"
    args={{
      helperText: "Make sure to fill this",
      label: "Helper Text"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-textarea label="Helper Text" helperText="Make sure to fill this"></scale-textarea>
```

## With Error

<Canvas withSource="none">
  <Story
    name="With Error"
    args={{
      label: "With Error",
      status: "error",
      helperText: "something is wrong"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-textarea label="With Error" helper-text="something is wrong" status="error"></scale-textarea>
```


## Disabled

<Canvas withSource="none">
  <Story
    name="Disabled"
    args={{
      label: "Disabled",
      disabled: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-textarea label="Disabled" disabled></scale-textarea>
```


## Max Length With Counter

<Canvas withSource="none">
  <Story
    name="Max Length With Counter"
    args={{
      label: "Max Length With Counter",
      maxLength: 10,
      counter: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-textarea label="Max Length With Counter" max-length="10" counter></scale-textarea>
```


## More rows

<Canvas withSource="none">
  <Story
    name="More rows"
    args={{
      label: "More rows",
      rows: 10
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```html
<scale-textarea label="More rows" rows="10"></scale-textarea>
```
