import { Meta, Story, Props, Preview, Description } from '@storybook/addon-docs/blocks';
import ScaleCard from './ScaleCard.vue';

<!--
  👀

  We're missing two key things from Stencil's docs in the generated readme file:
  - whether attributes are optional or required
  - any description for those attributes
  - plus, writing them again in the .vue file can be error-prone.

  Other minor pain-points:
  - writing the code-snippet manually, need to make sure attribute/values match
  - declaring all props both in Template and the <template> of the .vue file

  Two more thing to think about:
  - in the end there's nothing Vue-specific in the stories, only for us devs, no?
    al least until the code snippet in the Preview works as expected and doesn't show `Template.bind()`
  - we're not using the Vue output target package (it's not really useful in Storybook)
-->

<Meta
  title='Components/Card'
  component={ScaleCard}
  argTypes={{
    label: {
      control: { type: 'text' }
    },
    styles: {
      control: { disable: true }
    }
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { ScaleCard },
  props: {
    slot: String,
    ...ScaleCard.props
  },
  template: `
    <scale-card
      :custom-class="customClass"
      :href="href"
      :target="target"
      :interactive="interactive"
      :no-padding="noPadding"
      :disabled="disabled"
      :styles="JSON.stringify(styles)"
    >
      {{ slot }}
    </scale-card>
  `
})

# Card

## Standard

<Preview withSource="none">
  <Story
    name='Standard'
    args={{
        slot: 'A card',
        label: 'Example Card',
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props story='Standard' />

## With Link

<Preview withSource="none">
  <Story
    name='With Link'
    args={{
      slot: 'A card with a link to example.com',
      label: 'Example Card',
      href: 'https://example.com',
      target: '_blank'
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-card href="https://example.com" target="_blank" label="Example Card">A card with a link to example.com</scale-card>
```


## Padding Off

<Preview withSource="none">
  <Story
    name='Padding Off'
    args={{
      slot: 'A card with a link to example.com',
      label: 'Example Card',
      noPadding: true
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-card href="https://example.com" target="_blank" no-padding label="Example Card">A card with no padding</scale-card>
```

## Interactive

<Preview withSource="none">
  <Story
    name='Interactive'
    args={{
      slot: 'A card, interactive, no link',
      label: 'Example Card',
      interactive: true
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-card interactive label="Example Card">A card, interactive</scale-card>
```


## Interactive Padding Off

<Preview withSource="none">
  <Story
    name='Interactive Padding Off'
    args={{
      slot: 'A card, interactive, no link',
      label: 'Example Card',
      interactive: true,
      noPadding: true
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-card interactive no-padding label="Example Card">A card, interactive, no link</scale-card>
```

## Disabled

<Preview withSource="none">
  <Story
    name='Disabled'
    args={{
      slot: 'A card, disabled',
      label: 'Example Card',
      disabled: true
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

```html
<scale-card disabled label="Example Card">A card, interactive</scale-card>
```
