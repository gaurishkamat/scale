import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import TestButton from './TestButton.vue';
import WithArgs from './TestButton.stories';

<Meta title='Test Button' component={TestButton} />

export const Template = (args) => ({
  components: { TestButton },
  data() {
    return { args }
  },
  template: '<test-button v-bind="args"></test-button>'
})

# Test Button

I'm testing a real Vue component here.

<Preview>
  <Story name='Text'>{{
    components: { TestButton },
    template: '<test-button label="Click!"></test-button>',
  }}</Story>
</Preview>

<Preview>
  <Story name='With Args' args={{ label: 'OKCOOL' }}>
    {Template.bind(null)}
  </Story>
</Preview>

<p>&nbsp;</p>

## Props

This works… Also removing this MDX and adding `component: TestButton` in the default export of `TestButton.stories.js` will render this props table.

<Props of={TestButton} />

Docs: https://github.com/storybookjs/storybook/blob/next/addons/docs/vue/README.md#props-tables

Next steps (I think):
- decide whether we really need "props table" or can import and use Stencil's readme
- test "props table" for web components and evaluate going back to vanilla
- test args syntax with web components?