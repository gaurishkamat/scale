{
  "name": "@scaleds/components-sketch-telekom",
  "publishConfig": {
    "access": "restricted"
  },
  "version": "1.15.0",
  "description": "",
  "license": "MIT",
  "scripts": {
    "prestart": "rimraf public/components && rimraf public/icons && cp -r ../components/dist public/components && cp -r ../icons/dist public/icons",
    "start": "nodemon app.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-handlebars": "^5.1.0",
    "nodemon": "^2.0.4"
  }
}
